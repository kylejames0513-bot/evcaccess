"use server";

// signin_sessions table has been dropped — review_queue replaces it.
// This is a no-op stub to keep imports working.

export async function resolveSigninSessionAction(
  _formData: FormData,
): Promise<void> {
  // no-op
}
