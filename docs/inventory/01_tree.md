# 01 Tree

Two level file tree from repo root. Excludes `node_modules`, `.next`, `.vercel`, `dist`, `build`, `.git`, `coverage`.

```
.
.gitignore
Excel and Access Files/
    Access Database Module.bas
    Monthly New Hire Tracker Macro.bas
Google Sheets/
    Config.gs
    Core.gs
    EVC_Attendance_Tracker (2).xlsx
    SupabaseSync.gs
    Utilities.gs
docs/
    inventory/
training-hub/
    .env.local.example
    .gitignore
    AGENTS.md
    CLAUDE.md
    README.md
    eslint.config.mjs
    next.config.ts
    package-lock.json
    package.json
    postcss.config.mjs
    public/
    src/
    supabase/
    tsconfig.json
    vercel.json
```

Note: the actual Next.js app lives in `training-hub/`, not the repo root. All subsequent chunks treat `training-hub/` as the app root.
